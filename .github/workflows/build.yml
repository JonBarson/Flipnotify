name: Build FlipNotify
on:
  workflow_dispatch:
  push:
    branches: [ main ]
permissions:
  contents: write
jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }
      - uses: gradle/actions/setup-gradle@v4
        with: { gradle-version: '8.9' }
      - run: cd android && gradle assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: FlipNotify-android
          path: android/app/build/outputs/apk/debug/app-debug.apk
  flipper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: python -m pip install --upgrade pip ufbt
      - run: cd flipper && ufbt
      - uses: actions/upload-artifact@v4
        with:
          name: FlipNotify-flipper
          path: flipper/dist/flipnotify.fap
  release:
    needs: [ android, flipper ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
      - name: Publish APK and FAP to "latest" release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete latest --cleanup-tag --yes || true
          gh release create latest dist/FlipNotify-android/app-debug.apk dist/FlipNotify-flipper/flipnotify.fap -t "Latest build" -n "Automated APK + FAP build"
